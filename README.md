# Homelab

Infrastructure as code for homelab Docker stacks.

Managed by Komodo on `pv-nod1`.

## Structure

- `stacks/<name>/compose.yaml` — Docker Compose stack definitions

## Secrets

Secrets are managed via Komodo, never committed to this repository.# homelab
